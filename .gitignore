node_modules/
dist/
*.tsbuildinfo

# Environment
.env
.env.local

# Internal-only — these directories belong in the operator's private
# workspace at /Users/stepan/Source/leash/ and should never end up in
# this repo. Listing them here is defensive in case of an accidental copy.
summary/
poc/

# Claude Code: commit CLAUDE.md + the curated `.claude/` assets
# (settings.json, commands/, skills/), but ignore generated session state.
.claude/worktrees/
.claude/plans/
.claude/sessions/
.claude/settings.local.json

# Local state
.leash/

# Session keys (generated, contains private key material)
session-key.json

# OS
.DS_Store
Thumbs.db

# Foundry
contracts/out/
contracts/cache/
contracts/broadcast/**/dry-run/
poc/*/out/
poc/*/cache/
poc/*/broadcast/**/dry-run/

# SQLite
*.db
*.db-journal
*.db-wal
