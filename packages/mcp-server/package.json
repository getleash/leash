{
  "name": "@getleash/mcp-server",
  "version": "0.1.0",
  "description": "Leash MCP stdio proxy: 5 own tools + 10 x402 upstream adapters + SQLite payment log.",
  "license": "MIT",
  "author": "Stepan Kouba",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/getleash/leash.git",
    "directory": "packages/mcp-server"
  },
  "homepage": "https://getleash.dev",
  "bugs": "https://github.com/getleash/leash/issues",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "README.md", "LICENSE"],
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "test": "vitest run --exclude test/integration/",
    "test:sepolia": "vitest run test/integration/",
    "clean": "rm -rf dist",
    "prepack": "npm run clean && npm run build"
  },
  "dependencies": {
    "@getleash/core": "^0.1.0",
    "@getleash/bundler": "^0.1.0",
    "@modelcontextprotocol/sdk": "^1.11.0",
    "better-sqlite3": "^11.8.0",
    "viem": "^2.27.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "tsup": "^8.4.0",
    "vitest": "^3.1.0"
  }
}
