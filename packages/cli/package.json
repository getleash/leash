{
  "name": "@getleash/cli",
  "version": "0.1.0",
  "description": "Leash CLI: apply policies, run the MCP server, manage sub-accounts, fund and drain USDC.",
  "license": "MIT",
  "author": "Stepan Kouba",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/getleash/leash.git",
    "directory": "packages/cli"
  },
  "homepage": "https://getleash.dev",
  "bugs": "https://github.com/getleash/leash/issues",
  "type": "module",
  "bin": {
    "leash": "dist/index.js"
  },
  "files": ["dist", "README.md", "LICENSE"],
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist",
    "prepack": "npm run clean && npm run build"
  },
  "dependencies": {
    "@getleash/core": "^0.1.0",
    "@getleash/mcp-server": "^0.1.0",
    "@getleash/bundler": "^0.1.0",
    "@modelcontextprotocol/sdk": "^1.11.0",
    "hash-wasm": "^4.11.0",
    "viem": "^2.27.0"
  },
  "devDependencies": {
    "tsup": "^8.4.0",
    "vitest": "^3.1.0"
  }
}
